cache: apt
sudo: true
language: python
python:
  - "3.6"
#  - "3.5"
#addons:
#  apt:
#    packages:
#    - libblas-dev
#    - liblapack-dev
#    - gfortran
#    - libhdf5-serial-dev
#    - libboost-all-dev

before_install:
  - cd $TRAVIS_BUILD_DIR/install/3_0_install/savu_v3.0
  - ls -lah

install:
  - ./savu_installer.sh --no-prompts --PREFIX /tmp

script:
  - py.test scripts/configurator_tests/ -v
  - py.test savu/test/travis -v --cov savu --cov-report term-missing

after_script:
  - coveralls
  
#allow_failures:
#    - python: "3.5"

