checks:
    python:
        code_rating: true
        duplicate_code: true
build:
    environment:
        python:
            version: 3.7.5
            # Whether to setup a virtual environment in the repository root directory.
            # Defaults to true if omitted.
            virtualenv: false

filter:
    excluded_paths:
        - '*/test/*'
