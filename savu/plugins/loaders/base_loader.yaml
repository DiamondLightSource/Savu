---
      - verbose: A base class for loader plugins. A base plugin from which all data loader plugins should inherit.
        parameters:
           - preview:
                  visibility: user
                  type: '[int]'
                  description: A slice list of required frames.
                  default: []
           - in_datasets:
                  visibility: hide
                  type: list
                  description: Create a list of the dataset(s) to process
                  default: []
           - out_datasets:
                  visibility: hide
                  type: list
                  description: Create a list of the dataset(s) to create
                  default: []