from savu.plugins.plugin_tools import PluginTools

class TimeseriesFieldCorrectionsTools(PluginTools):
    """A Plugin to apply a simple dark and flatfield correction to some
raw timeseries data
    """