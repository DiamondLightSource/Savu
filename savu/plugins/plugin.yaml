---
- verbose: The base class from which all plugins should inherit.
  parameters:
      - in_datasets:
            visibility: param
            type: list
            description: Create a list of the dataset(s) to process
            default: []
      - out_datasets:
            visibility: param
            type: list
            description: Create a list of the dataset(s) to create
            default: []
